import { SortField, Category } from './types';

export const CATEGORIES: Category[] = [
	'all',
	'art',
	'biography',
	'computers',
	'history',
	'medical',
	'poetry',
];

export const SORT_FIELDS: SortField[] = ['relevance', 'newest'];
